// ReCaptcha placeholder removed — project no longer uses Google reCAPTCHA.
// Export a no-op component so imports (if left elsewhere) do not break the build.
const ReCaptcha = () => null;
export default ReCaptcha;
